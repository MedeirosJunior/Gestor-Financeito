services:
  - type: web
    name: gestor-financeito
    env: node
    plan: free
    buildCommand: npm install && cd gestor-financeiro-frontend && npm install && npm run build && cd ..
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
