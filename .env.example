# ============ SERVIDOR ============
NODE_ENV=production
PORT=10000

# ============ SEGURANÇA ============
# Chave secreta para assinar tokens JWT — use uma string longa e aleatória
JWT_SECRET=troque-por-uma-chave-secreta-longa-e-aleatoria

# ============ ADMINISTRADOR PADRÃO ============
# Usuário admin criado automaticamente na primeira execução
ADMIN_EMAIL=admin@exemplo.com
ADMIN_PASSWORD=senha-segura-aqui

# ============ BANCO DE DADOS — TURSO (produção) ============
# Obtenha no painel: https://turso.tech
TURSO_DATABASE_URL=libsql://seu-banco.turso.io
TURSO_AUTH_TOKEN=seu-token-do-turso

# ============ FRONTEND ============
# URL do frontend para configuração de CORS
FRONTEND_URL=https://gestor-financeito.netlify.app

# ============ E-MAIL — SENDGRID (recomendado para Render free) ============
# Obtenha em: https://sendgrid.com
SENDGRID_API_KEY=SG.xxxxx
SENDGRID_FROM=noreply@seudominio.com

# ============ E-MAIL — SMTP (alternativa ao SendGrid) ============
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=seuemail@gmail.com
SMTP_PASS=sua-senha-de-app-google
SMTP_FROM=seuemail@gmail.com

# ============ LICENÇA — MYSQL CENTRAL (opcional) ============
# Se informado, valida a licença cruzando o banco local (licenca_local) com o
# banco central MySQL (controle_clientes). Sem essas variáveis, a validação de
# licença é ignorada e o sistema roda normalmente.
# MYSQL_HOST=seu-servidor-mysql.com.br
# MYSQL_PORT=3306
# MYSQL_DATABASE=controle_clientes
# MYSQL_USER=usuario_licencas
# MYSQL_PASSWORD=senha_super_segura_aqui
# MYSQL_SSL=false

# CNPJ e código do cliente (usados quando não há licença_local cadastrada)
# CLIENTE_CNPJ=00.000.000/0001-00
# CLIENTE_CODIGO=1
